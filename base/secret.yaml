#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@ prefix = data.values.prefix or ""

---
apiVersion: v1
data:
  db-password: MTIzNDU=
kind: Secret
metadata:
  name: #@ prefix + "spring-petclinic-secret"
type: Opaque