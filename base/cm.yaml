#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@ prefix = data.values.prefix or ""

---
apiVersion: v1
kind: ConfigMap
data:
  RUNTIME_ENV: #@ data.values.env
metadata:
  name: #@ prefix + str("spring-petclinic-cm")
  labels: 
     service_label:  #@ prefix + str("cm")
